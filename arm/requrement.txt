# --- For Lambda Function (arm/src) ---

# HTTP requests (for GitHub API, Docker manifests, PyPI, NPM)
requests

# Python packaging utilities (for Python dependency analyzer)
packaging

# Semantic versioning (for JS dependency analyzer)
semver
